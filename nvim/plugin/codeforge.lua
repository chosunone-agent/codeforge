-- CodeForge plugin entry point
-- This file ensures the plugin directory is recognized by Neovim
-- Actual setup happens via require("codeforge").setup()

-- Note: For :checkhealth to work before explicitly loading the plugin,
-- configure lazy.nvim with lazy = false or event = "VeryLazy"
