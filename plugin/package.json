{
  "name": "suggestion-manager",
  "version": "0.1.0",
  "description": "OpenCode plugin for managing code suggestions with hunk-by-hunk review",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.1.3",
    "@opencode-ai/sdk": "^1.1.3",
    "@types/bun": "latest",
    "typescript": "^5.9.3",
    "zod": "^4.3.5"
  }
}
